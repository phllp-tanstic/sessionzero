# TEST FIXTURES — NOT LIVE DATA

Every payload in this directory is deterministic test input. Production code cannot import or read
this directory, and no fixture is presented as current market data.

